const discord = require("discord.js")
const db = require("quick.db")

//-----------------------------------------

module.exports = {
    name: "apply",
    allies: ["request"],
    description: "Apply for jobs Using this command, To check the jobs Write: ``(prefix)jobs",

    async run (client, message, args) {







}

}